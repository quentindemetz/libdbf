## Process this file with automake to produce Makefile.in

pslibincdir = $(includedir)/libdbf

pslibinc_HEADERS = \
	libdbf/libdbf.h 

install-exec-hook:
	$(mkinstalldirs) $(DESTDIR)$(libdbfincdir)

EXTRA_DIST = 
